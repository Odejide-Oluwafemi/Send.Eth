const Logo = () => {
  return (
    <div id="logo">
      <span id="logo-send">Send</span>
      <span id="logo-dot"></span>
      <span id="logo-eth">ETH</span>
    </div>
  );
};

export default Logo;
