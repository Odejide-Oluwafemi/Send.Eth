const Logo = () => {
  return (
    <a href="#">
      <div id="logo">
        <span id="logo-send">Send</span>
        <span id="logo-dot" className="bg-orangeAccent"></span>
        <span id="logo-eth">ETH</span>
      </div>
    </a>
  );
};

export default Logo;
